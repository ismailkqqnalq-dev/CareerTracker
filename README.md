# CareerTracker
making easier to track your career with python

FastAPI tabanlı CareerTracker projesi için başlangıç iskeleti.

## Yapı

- `app/`: Uygulama kodu
- `tests/`: Testler
- `migrations/`: Veritabanı migration dosyaları

## Çalıştırma

```bash
uvicorn app.main:app --reload
```
